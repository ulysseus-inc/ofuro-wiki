query adminBackupList($skip: Int, $take: Int) {
  adminBackupList(skip: $skip, take: $take) {
    items {
      id
      filename
      size
      workspaceCount
      docCount
      blobCount
      status
      createdAt
    }
    totalCount
  }
}
